# These are combined (CFHT + MOIRCS) images for J and K

# H-band is only from MOIRCS -- shallower than J and K

# Images are in microJy ==> zeropoint 23.9 

# Catalog is K-selected (DETECT_MINAREA=4 pix, DETECT_THRESH=2 sigma)
within GOODS-N ACS area
